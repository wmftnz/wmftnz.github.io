.DS_Store
node_modules/

# Contains unredacted customer PII — use assets/studio-admin.jpg (blurred) instead
assets/studio-admin-unblurred.jpg

# Camera/screenshot originals kept locally; web-sized copies live in assets/
# (HEIC doesn't render in Chrome or Firefox, so it can never ship as-is).
*.HEIC
dryhirecodebackend.jpg
